TODO(ui-audio): add `reverse_alarm_ring.mp3` here and mirror it to `android/app/src/main/res/raw/reverse_alarm_ring.mp3`.

Current runtime behavior falls back to default notification sound plus best-effort in-app alert/vibration when the custom asset is missing.
